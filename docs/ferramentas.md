# Ferramentas

Tribunal TRF6: Processo expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_trf6_processo_consultar`
**Input**: `numero_processo` (opcional), `nome_parte` (opcional), `nome_advogado` (opcional), `cpf` (opcional), `cnpj` (opcional)

Tribunal TRF6: Processo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TRF6: Processo
```
